---
import LayoutV2 from "@/layouts/LayoutV2.astro";
import { Icon } from "astro-icon/components";

const jobs = [
  {
    id: "frontend-engineer",
    title: "Frontend Engineer",
    location: "Remote",
    type: "Full-time",
    department: "Engineering",
    description:
      "Build intuitive interfaces for AI platform management, monitoring, and governance. Create tools that make complex AI operations simple for enterprise users.",
    requirements: [
      "3+ years experience with React/TypeScript",
      "Experience with data visualization and dashboards",
      "Understanding of design systems and UI/UX principles",
      "Bonus: Experience with AI/ML tools or platforms",
    ],
    benefits: [
      "Work on greenfield projects",
      "Collaborate with product and design",
      "Full remote flexibility",
      "Modern tech stack and best practices",
    ],
    applyLink: "mailto:careers@bitropy.io?subject=Application: Frontend Engineer",
  },
];
---

<LayoutV2 title="Jobs">
  <!-- Hero Section -->
  <section class="relative py-24 bg-gradient-to-b from-light-bg via-light-accent to-light-bg overflow-hidden">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center relative z-10">
      <div class="inline-block px-4 py-2 mb-6 rounded-full bg-brand/10 border border-brand/20">
        <span class="text-brand text-sm font-medium uppercase tracking-wider">Careers</span>
      </div>

      <h1 class="text-5xl md:text-6xl font-bold mb-6">Join Our Team</h1>
      <p class="text-xl text-text-secondary max-w-3xl mx-auto">
        Help us unlock production AI for regulated industries
      </p>
    </div>

    <!-- Background decoration -->
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-brand/5 rounded-full blur-3xl -z-10">
    </div>
  </section>

  <!-- Introduction -->
  <section class="py-16 bg-light-bg">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="space-y-6 text-lg text-text-secondary leading-relaxed">
        <p>
          We're building the future of sovereign AI infrastructure. Join a team of passionate engineers and
          problem-solvers who believe enterprises deserve AI that works for them—not the other way around.
        </p>
        <p>
          At Bitropy, you'll work on cutting-edge technology that's making real impact in pharma, banking,
          and other regulated industries.
        </p>
      </div>
    </div>
  </section>

  <!-- Current Openings -->
  <section class="py-24 bg-gradient-to-b from-light-bg to-light-accent">
    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-4xl font-bold mb-12 text-center">Current Openings</h2>

      {
        jobs.length === 0 ? (
          <div class="bg-light-accent/50 border border-brand/20 rounded-2xl p-12 text-center">
            <Icon class="w-16 h-16 text-brand/50 mx-auto mb-4" name="bx:bx-briefcase" />
            <h3 class="text-2xl font-semibold mb-2">No open positions right now</h3>
            <p class="text-text-secondary mb-6">
              We're always looking for talented people. Send us your CV at{" "}
              <a href="mailto:careers@bitropy.io" class="text-brand hover:text-brand-dark underline">
                careers@bitropy.io
              </a>
            </p>
          </div>
        ) : (
          <div class="space-y-6">
            {jobs.map((job) => (
              <div class="group bg-light-accent/50 border border-brand/20 rounded-2xl p-8 hover:border-brand/40 hover:shadow-2xl hover:shadow-[#1f2937]/10 transition-all">
                <div class="flex flex-col md:flex-row md:items-start md:justify-between gap-6 mb-6">
                  <div class="flex-1">
                    <h3 class="text-3xl font-bold mb-3 text-text-primary">{job.title}</h3>
                    <div class="flex flex-wrap gap-4 text-sm text-text-secondary">
                      <span class="inline-flex items-center gap-2">
                        <Icon class="w-4 h-4 text-brand" name="bx:bx-map" />
                        {job.location}
                      </span>
                      <span class="inline-flex items-center gap-2">
                        <Icon class="w-4 h-4 text-brand" name="bx:bx-time" />
                        {job.type}
                      </span>
                      <span class="inline-flex items-center gap-2">
                        <Icon class="w-4 h-4 text-brand" name="bx:bx-briefcase" />
                        {job.department}
                      </span>
                    </div>
                  </div>
                  <a
                    href={job.applyLink}
                    class="inline-flex items-center justify-center gap-2 px-6 py-3 bg-brand hover:bg-brand-dark text-white rounded-lg font-semibold transition-all shadow-lg shadow-[#1f2937]/20 whitespace-nowrap">
                    Apply Now
                    <Icon class="w-4 h-4" name="bx:bx-right-arrow-alt" />
                  </a>
                </div>

                <p class="text-text-secondary leading-relaxed mb-6">{job.description}</p>

                <div class="grid md:grid-cols-2 gap-8">
                  <div>
                    <h4 class="font-semibold text-lg mb-4 flex items-center gap-2 text-text-primary">
                      <Icon class="w-5 h-5 text-brand" name="bx:bx-check-circle" />
                      What we're looking for
                    </h4>
                    <ul class="space-y-2">
                      {job.requirements.map((req) => (
                        <li class="text-text-secondary flex items-start gap-2">
                          <span class="text-brand mt-1">•</span>
                          <span>{req}</span>
                        </li>
                      ))}
                    </ul>
                  </div>
                  <div>
                    <h4 class="font-semibold text-lg mb-4 flex items-center gap-2 text-text-primary">
                      <Icon class="w-5 h-5 text-brand" name="bx:bx-star" />
                      What we offer
                    </h4>
                    <ul class="space-y-2">
                      {job.benefits.map((benefit) => (
                        <li class="text-text-secondary flex items-start gap-2">
                          <span class="text-brand mt-1">•</span>
                          <span>{benefit}</span>
                        </li>
                      ))}
                    </ul>
                  </div>
                </div>
              </div>
            ))}
          </div>
        )
      }
    </div>
  </section>

  <!-- Why Bitropy -->
  <section class="py-24 bg-light-bg">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-4xl font-bold mb-12 text-center">Why Work at Bitropy?</h2>

      <div class="grid md:grid-cols-3 gap-8">
        <div class="bg-light-accent/50 border border-brand/20 rounded-2xl p-8">
          <div class="w-12 h-12 rounded-lg bg-brand/20 flex items-center justify-center mb-6">
            <Icon class="w-6 h-6 text-brand" name="bx:bxs-rocket" />
          </div>
          <h3 class="text-2xl font-semibold mb-4 text-text-primary">Cutting-Edge Technology</h3>
          <p class="text-text-secondary leading-relaxed">
            Work with the latest in AI, Kubernetes, and distributed systems. Shape the future of sovereign AI.
          </p>
        </div>

        <div class="bg-light-accent/50 border border-brand/20 rounded-2xl p-8">
          <div class="w-12 h-12 rounded-lg bg-brand/20 flex items-center justify-center mb-6">
            <Icon class="w-6 h-6 text-brand" name="bx:bxs-group" />
          </div>
          <h3 class="text-2xl font-semibold mb-4 text-text-primary">Talented Team</h3>
          <p class="text-text-secondary leading-relaxed">
            Join passionate builders and problem-solvers who've spent years inside enterprise AI.
          </p>
        </div>

        <div class="bg-light-accent/50 border border-brand/20 rounded-2xl p-8">
          <div class="w-12 h-12 rounded-lg bg-brand/20 flex items-center justify-center mb-6">
            <Icon class="w-6 h-6 text-brand" name="bx:bxs-like" />
          </div>
          <h3 class="text-2xl font-semibold mb-4 text-text-primary">Real Impact</h3>
          <p class="text-text-secondary leading-relaxed">
            Your work directly enables pharma companies, banks, and enterprises to innovate with AI.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Spontaneous Applications -->
  <section class="py-24 bg-gradient-to-b from-light-bg to-light-accent">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-4xl font-bold mb-4">Don't see your role?</h2>
      <p class="text-lg text-text-secondary mb-8">
        We're always interested in hearing from talented people. Send us your CV and tell us why you want to
        join Bitropy.
      </p>
      <a
        href="mailto:careers@bitropy.io"
        class="inline-flex items-center gap-2 px-8 py-4 bg-brand hover:bg-brand-dark text-white rounded-lg font-semibold transition-all shadow-lg shadow-[#1f2937]/20">
        <Icon class="w-5 h-5" name="bx:bx-envelope" />
        careers@bitropy.io
      </a>
    </div>
  </section>
</LayoutV2>
