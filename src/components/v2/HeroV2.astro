---
import { Icon } from "astro-icon/components";
---

<section class="relative min-h-[90vh] flex items-center justify-center overflow-hidden bg-gradient-to-b from-light-bg via-light-accent to-light-bg">

  <!-- Content -->
  <div class="relative z-10 max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <div class="inline-block px-4 py-2 mb-6 rounded-full bg-brand/10 border border-brand/20">
      <span class="text-brand text-sm font-medium">✨ Next-Gen AI Infrastructure</span>
    </div>

    <h1 class="text-5xl md:text-6xl lg:text-7xl font-bold mb-6 leading-tight">
      Orchestrate Your Future with
      <span class="block mt-2 bg-gradient-to-r from-brand via-brand-dark to-brand bg-clip-text text-transparent">
        Sovereign AI
      </span>
    </h1>

    <p class="text-xl md:text-2xl text-text-secondary mb-10 max-w-3xl mx-auto leading-relaxed">
      Deploy a Kubernetes-based AI platform, completely on-premises, and stay compliant
      with GDPR, BaFin, and the EU AI Act from day one.
    </p>

    <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
      <a
        href="/v2/contact"
        class="inline-flex items-center gap-2 px-8 py-4 bg-brand hover:bg-brand-dark text-white rounded-lg font-semibold transition-all transform hover:scale-105 shadow-lg shadow-[#1f2937]/20">
        <Icon class="w-5 h-5" name="bx:bx-rocket" />
        Start Free Trial
      </a>
      <a
        href="#demo"
        class="inline-flex items-center gap-2 px-8 py-4 bg-transparent border-2 border-brand/30 hover:border-brand text-text-primary rounded-lg font-semibold transition-all">
        <Icon class="w-5 h-5" name="bx:bx-play-circle" />
        Watch Demo
      </a>
    </div>

    <!-- Trust badges -->
    <div class="mt-16 flex flex-wrap justify-center items-center gap-8 opacity-60">
      <div class="flex items-center gap-2">
        <Icon class="w-5 h-5 text-brand" name="bx:bx-check-shield" />
        <span class="text-sm text-text-secondary">GDPR Compliant</span>
      </div>
      <div class="flex items-center gap-2">
        <Icon class="w-5 h-5 text-brand" name="bx:bx-lock-alt" />
        <span class="text-sm text-text-secondary">On-Premises</span>
      </div>
      <div class="flex items-center gap-2">
        <Icon class="w-5 h-5 text-brand" name="bx:bx-rocket" />
        <span class="text-sm text-text-secondary">Deploy in 2 Weeks</span>
      </div>
    </div>
  </div>

  <!-- Gradient overlay at bottom -->
  <div class="absolute bottom-0 left-0 right-0 h-32 bg-gradient-to-t from-light-bg to-transparent"></div>
</section>

