---
import { Icon } from "astro-icon/components";
---

<section class="py-24 bg-gradient-to-b from-dark-bg to-dark-accent">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid lg:grid-cols-2 gap-16 items-center">
      <!-- Left: Text Content -->
      <div>
        <div class="inline-block px-4 py-2 mb-6 rounded-full bg-brand/10 border border-brand/20">
          <span class="text-brand text-sm font-medium uppercase tracking-wider">Intelligent Solutions</span>
        </div>

        <h2 class="text-4xl md:text-5xl font-bold mb-6">
          Transforming Business Efficiency
        </h2>

        <p class="text-xl text-gray-300 mb-8 leading-relaxed">
          Bitropy delivers a Kubernetes-native AI platform that scales production workloads
          while keeping everything under your control—no data leaks, no vendor lock-in.
        </p>

        <ul class="space-y-4">
          <li class="flex items-start gap-3">
            <div class="flex-shrink-0 w-6 h-6 rounded-full bg-brand/20 flex items-center justify-center mt-1">
              <Icon class="w-4 h-4 text-brand" name="bx:bx-check" />
            </div>
            <span class="text-gray-300">Deploy in under 2 weeks, not 6-12 months</span>
          </li>
          <li class="flex items-start gap-3">
            <div class="flex-shrink-0 w-6 h-6 rounded-full bg-brand/20 flex items-center justify-center mt-1">
              <Icon class="w-4 h-4 text-brand" name="bx:bx-check" />
            </div>
            <span class="text-gray-300">70% more cost-effective than traditional consulting</span>
          </li>
          <li class="flex items-start gap-3">
            <div class="flex-shrink-0 w-6 h-6 rounded-full bg-brand/20 flex items-center justify-center mt-1">
              <Icon class="w-4 h-4 text-brand" name="bx:bx-check" />
            </div>
            <span class="text-gray-300">Full compliance with GDPR, BaFin, and EU AI Act</span>
          </li>
          <li class="flex items-start gap-3">
            <div class="flex-shrink-0 w-6 h-6 rounded-full bg-brand/20 flex items-center justify-center mt-1">
              <Icon class="w-4 h-4 text-brand" name="bx:bx-check" />
            </div>
            <span class="text-gray-300">Sovereign by design—your data never leaves</span>
          </li>
        </ul>
      </div>

      <!-- Right: Isometric Illustration (using icons as placeholder) -->
      <div class="relative">
        <div class="relative aspect-square max-w-lg mx-auto">
          <!-- Central Chip/Processor -->
          <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-48 h-48">
            <div class="relative w-full h-full">
              <!-- Main processor -->
              <div class="absolute inset-0 bg-gradient-to-br from-brand via-brand-dark to-brand rounded-2xl shadow-2xl shadow-[#A898E9]/40 transform rotate-45">
                <div class="absolute inset-4 bg-dark-accent rounded-xl flex items-center justify-center -rotate-45">
                  <Icon class="w-24 h-24 text-brand" name="bx:bxs-chip" />
                </div>
              </div>

              <!-- Circuit lines -->
              <div class="absolute -top-20 left-1/2 w-1 h-20 bg-gradient-to-t from-brand to-transparent"></div>
              <div class="absolute -bottom-20 left-1/2 w-1 h-20 bg-gradient-to-b from-brand to-transparent"></div>
              <div class="absolute -left-20 top-1/2 h-1 w-20 bg-gradient-to-l from-brand to-transparent"></div>
              <div class="absolute -right-20 top-1/2 h-1 w-20 bg-gradient-to-r from-brand to-transparent"></div>
            </div>
          </div>

          <!-- Floating Elements -->
          <!-- Top -->
          <div class="absolute top-0 left-1/2 -translate-x-1/2 w-20 h-20 bg-dark-accent border border-brand/30 rounded-lg flex items-center justify-center shadow-lg shadow-[#A898E9]/20 animate-float">
            <Icon class="w-10 h-10 text-brand" name="bx:bxs-server" />
          </div>

          <!-- Bottom -->
          <div class="absolute bottom-0 left-1/2 -translate-x-1/2 w-20 h-20 bg-dark-accent border border-brand/30 rounded-lg flex items-center justify-center shadow-lg shadow-[#A898E9]/20 animate-float" style="animation-delay: 0.5s;">
            <Icon class="w-10 h-10 text-brand" name="bx:bxs-data" />
          </div>

          <!-- Left -->
          <div class="absolute top-1/2 left-0 -translate-y-1/2 w-20 h-20 bg-dark-accent border border-brand/30 rounded-lg flex items-center justify-center shadow-lg shadow-[#A898E9]/20 animate-float" style="animation-delay: 1s;">
            <Icon class="w-10 h-10 text-brand" name="bx:bxs-lock-alt" />
          </div>

          <!-- Right -->
          <div class="absolute top-1/2 right-0 -translate-y-1/2 w-20 h-20 bg-dark-accent border border-brand/30 rounded-lg flex items-center justify-center shadow-lg shadow-[#A898E9]/20 animate-float" style="animation-delay: 1.5s;">
            <Icon class="w-10 h-10 text-brand" name="bx:bxs-cloud" />
          </div>

          <!-- Glow effect -->
          <div class="absolute inset-0 bg-brand/10 rounded-full blur-3xl"></div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes float {
    0%,
    100% {
      transform: translateY(0) translateX(0);
    }
    50% {
      transform: translateY(-10px) translateX(5px);
    }
  }

  .animate-float {
    animation: float 3s ease-in-out infinite;
  }
</style>
